services:
  app:
    build:
      context: .
      dockerfile: infra/docker/app.Dockerfile
    container_name: miyin-tg-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "${WEB_PORT:-9800}:${WEB_PORT:-9800}"
